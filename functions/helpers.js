// CORS configuration
const corsConfig = {
    headers: {
        "Access-Control-Allow-Origin": "*",
        "Access-Control-Allow-Credentials": true,
    }
};

// Exporting the CORS configuration
module.exports = {
    corsConfig
}
